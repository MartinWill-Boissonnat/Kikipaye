{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<h1 class="text-center">Kikipaye</h1>
<p class="text-center"> Bienvenue sur Kikipaye, organiseur de budget pour projet entre amis, collègues, famille.. Cliquez sur le boutton "commencer" ci-dessous pour commencer un nouveau budget.</p>

<div class="contenaire text-center">
  <div class="example-wrapper">
    <table class="table">
      <tbody>
            {% for group in groups %}
              <tr>
                <td>{{ group.string }}</td> <!--Est déclarer dans le controlleur Groupe.php-->
                <td><a href="{{ path('groupe_show', {'id' : group.id} )}}" class="btn-btn-primary">Détails</a></td>
              </tr>
            {% endfor %}
      </tbody>
    </table>
  </div>
</div>

    <!-- Affiche le résultat de la variable -->
<div class="contenaire text-center">
      <a href="{{ path ('admin')}}" class="btn btn-danger">Page administration</a>
</div>

{% endblock %}
