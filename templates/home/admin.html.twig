
{% extends 'base.html.twig' %}
{% block title %}Kikipaye{% endblock %}



{% block body %}

<div class="example-wrapper">
    <h1 class="text-center">Budget</h1>
    {{dump(group)}}

      <table class="table text-center">
        <thead>
           <tr>
            <th>Participant</th>
            <th>Participation</th>
            <th>Balance</th>
          </tr>
        </thead>
       <tbody>
         {% for user in group.users %}
           <tr>
              <td> {{ user.name|upper}}</td>
              <td> <input type="text" value="{{ user.spending }}"/>€</td>
           </tr>
          {% endfor %}
       </tbody>
      </table>
    </div>


      <div class="container text-center">
        <div class="row">
          <div class="col-md-12">
            <a href="{{path('home')}}" class="btn btn-primary"> Retour au budget</a>
            <a href="#" class="btn btn-primary"> Calculer un nouveau budget</a>
            <a href="{{path('groupe_index')}}" class="btn btn-primary"> Ajouter quelqun dans le groupe</a>
          </div>
        </div>
    </div>
</div>
{% endblock %}
